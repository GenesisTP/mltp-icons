{% for image in images -%}
.flair--{{ image.filename|replace(".png","")|replace(".jpg", "") }} { background-position: {{ image.x ~ ('px' if image.x) }} {{ image.y ~ ('px' if image.y) }}; }
{% endfor %}
